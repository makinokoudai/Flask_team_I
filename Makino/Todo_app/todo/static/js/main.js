
var button = document.getElementsByClassName('edit_btn');

for (i = 0; i < button.length; i++) {
button[i].addEventListener("click", function() {
    // ボタンの要素を取得して、<p>タグから<input>タグに変更して、入力をする。
    // その後、Flaskの方でデータに反映させる。
});
}
